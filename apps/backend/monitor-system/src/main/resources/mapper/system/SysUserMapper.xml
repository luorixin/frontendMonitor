<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monitor.system.mapper.SysUserMapper">

  <resultMap id="SysUserResult" type="com.monitor.system.domain.SysUser">
    <id property="id" column="id"/>
    <result property="username" column="username"/>
    <result property="email" column="email"/>
    <result property="password" column="password"/>
    <result property="nickname" column="nickname"/>
    <result property="avatar" column="avatar"/>
    <result property="status" column="status"/>
    <result property="delFlag" column="del_flag"/>
    <result property="loginIp" column="login_ip"/>
    <result property="loginDate" column="login_date"/>
    <result property="createTime" column="create_time"/>
    <result property="updateTime" column="update_time"/>
  </resultMap>

  <sql id="selectSysUser">
    SELECT id, username, email, password, nickname, avatar, status, del_flag,
           login_ip, login_date, create_time, update_time
    FROM sys_user
  </sql>

  <select id="selectUserByUsername" parameterType="string" resultMap="SysUserResult">
    <include refid="selectSysUser"/>
    WHERE username = #{username} AND del_flag = 0
  </select>

  <select id="selectUserByEmail" parameterType="string" resultMap="SysUserResult">
    <include refid="selectSysUser"/>
    WHERE email = #{email} AND del_flag = 0
  </select>

  <select id="selectUserById" parameterType="long" resultMap="SysUserResult">
    <include refid="selectSysUser"/>
    WHERE id = #{id} AND del_flag = 0
  </select>
</mapper>
